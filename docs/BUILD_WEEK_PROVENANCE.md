# Build Week provenance

CommitGate was developed as a Build Week extension to an existing private AgentGate proof rather than as a clean-room standalone platform.

## Historical lineage anchors

- Parent: `adamblackoak/agentgate` (private)
- Baseline: `8b61015037f7e6d0cb4fa21517f4c6af059f6942`
- Build Week branch: `buildweek/commitgate`
- Submission-candidate tag: `buildweek-commitgate-submission`

The original Build Week delta added canonical proposal identity, authority binding, immediate pre-consequence validation, single-use execution tokens, bound receipts, duplicate safeguards and adversarial verification while preserving the parent chassis.

## Extraction rule

This bounded package is not represented as the complete historical repository. It is a deliberately reduced publication artifact whose purpose is to expose and verify the CommitGate contribution while keeping unrelated AgentGate parent capability private.

## Evidence discipline

Private Git commit history is lineage evidence. Publication of this extraction would establish public availability only from its publication event. A future release should record its release commit/hash and any independent archive identifier separately from the historical private commit references above.
